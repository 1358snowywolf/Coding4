#include <bits/stdc++.h>

using namespace std;

int main() {
	int N;
	cin >> N;
	vector<string> ascending;
	vector<string> descending;

	for(int i = 0; i < N; i++){
		string str;
		cin >> str;
		auto str2 = str;

		ascending.push_back(sort(str.begin(), str.end()));
	}

	return 0;
}
