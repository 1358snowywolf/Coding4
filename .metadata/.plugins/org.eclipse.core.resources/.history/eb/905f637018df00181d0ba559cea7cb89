#include <bits/stdc++.h>

using namespace std;

int main() {
	vector<tuple<int, int, int>> skyline;
	int start, height, end;

	while(cin >> start){
		cin >> height >> end;
		skyline.push_back(make_tuple(start, height, end));
	}
	return 0;
}
