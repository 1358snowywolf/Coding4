#include <bits/stdc++.h>

using namespace std;

int main() {
	int C;
	for(int i = 0; i < C; i++){
		int N, M;
		cin >> N >> M;
		vector<vector<int>> maze;

		for(int i = 0; i < N; i++){
			vector<int> line;

			for(int j = 0; j < M; j++){
				int input;
				cin >> input;
				line.push_back(input);
			}

			maze.push_back(line);
		}


	}
	return 0;
}
